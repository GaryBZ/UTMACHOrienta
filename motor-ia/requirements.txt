# Motor IA — UTMACHOrienta
# Pipeline completo: ingesta + retrieval + API

# ── Extracción de PDFs ──
pdfplumber>=0.10.0
pypdf>=4.0.0

# ── Chunking ──
langchain-text-splitters>=0.2.0

# ── Vector store ──
chromadb>=0.5.0

# ── LM Studio / OpenAI-compatible ──
openai>=1.0.0

# ── API ──
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic[email]>=2.0.0

# ── Base de datos ──
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# ── Auth ──
bcrypt>=4.0.0
pyjwt>=2.8.0

# ── Utilidades ──
python-dotenv>=1.0.0